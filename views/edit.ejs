<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-end ">
            <a class="bg-red-600 px-3  py-2 rounded-md mb-2" href="/logout">Logout</a>
        </div>
        <h5 class=" text-3xl mb-2">Edit Your Post</h5>
        <form method="post" action="/edit/<%= post._id %>">
            <textarea class="w-1/3 block mb-2 resize-none rounded-md outline-none bg-transparent border-2 border-zinc-800 px-3  py-2" placeholder="Whats on ur mind??" name="content" ><%= post.content %></textarea>
            <input class="block bg-yellow-600 text-black px-2 w-40 py-2 rounded-md " type="submit" value="Edit Your Post">
        </form>


    </div>
</body>
</html>